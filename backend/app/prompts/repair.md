Your previous script failed.

Script:
```python
{code}
```

Error:
```
{error}
```

Fix the error and return the COMPLETE corrected script as exactly one ```python block. Do not change the design intent.

If the error mentions fillet or chamfer ("no suitable edges", "BRep_API: command not done"), REMOVE that .fillet()/.chamfer() call entirely instead of retrying it differently.
